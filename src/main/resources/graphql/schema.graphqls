enum Attendance {
    ATTENDING
    NOT_ATTENDING
    MAYBE
    NO_RESPONSE
}

type Event {
    id: ID!
    title: String
    description: String
    date_of_event: String
}

type Invitation {
    id: ID!
    person_id: Int
    event_id: Int
    attendance: Attendance
}

type Query {
    allEvents: [Event]
    getInvitationById(id: Int): Invitation
}

type Mutation {
    setAttendance(attendance: Attendance, invitation_id: Int): Int
}

schema {
    query: Query
}